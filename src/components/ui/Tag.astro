---
interface Props {
  href?: string;
  class?: string;
  [key: string]: any;
}

const {
  href,
  class: className = '',
  ...rest
} = Astro.props;

const classes = `tag ${className}`.trim();

const Tag = href ? 'a' : 'span';
---

<Tag href={href} class={classes} {...rest}>
  <slot />
</Tag>
